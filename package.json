{
  "name": "daveenci-crm",
  "version": "1.0.0",
  "description": "A simple, clean CRM system built with React, Node.js, Prisma, and PostgreSQL",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm start --prefix src\"",
    "build": "npm install && npm install --prefix server && npm run build --prefix server",
    "start": "npm start --prefix server",
    "start:frontend": "npm start --prefix src",
    "setup": "npm install && npm install --prefix server && npm install --prefix src",
    "db:migrate": "npm run db:migrate --prefix server",
    "db:studio": "npm run db:studio --prefix server",
    "db:deploy-preserve": "npm run db:deploy-preserve --prefix server",
    "db:super-nuclear": "npm run db:super-nuclear --prefix server",
    "db:push": "npm run db:push --prefix server",
    "build:prod": "npm run setup && npm run build:prod --prefix server",
    "start:prod": "npm run start:prod --prefix server"
  },
  "keywords": [
    "crm",
    "react",
    "nodejs",
    "prisma",
    "postgresql"
  ],
  "author": "Daveenci",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
